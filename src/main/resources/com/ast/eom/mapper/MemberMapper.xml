<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ast.eom.dao.MemberDao">

	<!-- 컬럼 이름과 자바 객체의 프로퍼티 이름을 연결한다. -->
	<resultMap type="Member" id="MemberMap">
		<result column="mem_no" property="memberNo" />
		<result column="mem_type_no" property="memberTypeNo" />
		<result column="reg_date" property="registeredDate" />
		<result column="gender" property="gender" />
		<result column="dob" property="dateOfBirth" />
		<result column="id" property="id" />
		<result column="email" property="email" />
		<result column="name" property="name" />
		<result column="pwd" property="password" />
		<result column="addr_city" property="addressCity" />
		<result column="addr_sub" property="addressSuburb" />
		<result column="tel" property="tel" />
		<result column="profile_photo" property="profilePhoto" />
		<collection property="MemberType" resultMap="MemberMap"/>
	</resultMap>

	<resultMap type="MemberType" id="MemberTypeMap">
		<result column="mem_type_no" property="memberTypeNo" />
		<result column="mem_type_name" property="memberTypeName" />
	</resultMap>

	<select id="findAll" resultMap="MemberMap">
		select
		 m.mem_no, m.name, m.gender, m.addr_city, m.addr_sub, m.profile_photo, mt.mem_type_name
		from
		  member m
		inner join 
		  member_type mt
		on
		  m.mem_type_no = mt.mem_type_no 
	</select>
	
	
	
	

</mapper>